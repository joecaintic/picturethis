<li class="<%= self_or_other(message) %>">
  <div class="avatar">
    <img src="http://placehold.it/50x50" />
  </div>
  <div class="chatboxmessagecontent">
    <canvas id = <%= "here-#{message.id}"%> >
    </canvas>
    <script>
    var canvas = document.getElementById(<%= "'here-#{message.id}'".html_safe %>);
    Meme('<%= image_url (message.image) %>', canvas, "<%= message.body %>", '<%= message.body2 %>');
    </script>
    <time datatime="<%= message.created_at %>" title="<%= message.created_at.strftime("%d %b %Y at %I:%M%p") %>">
      <%= message_interlocutor(message).name %> • <%= message.created_at.strftime("%H:%M %p") %>
    </time>
  </div>
</li>
